# 配置说明（系统文件）

AI Copilot 的所有配置均保存在**本机**，不上传任何服务器。以下逐项说明各配置的位置、字段与含义。

## 1. AI 配置（ai-config.json）

路径：`~/Library/Application Support/AI Copilot/ai-config.json`

结构：

```json
{
  "profiles": [
    {
      "id": "uuid",
      "provider": "deepseek",
      "providerName": "DeepSeek",
      "type": "openai",
      "baseUrl": "https://api.deepseek.com/v1",
      "apiKey": "sk-...",
      "model": "deepseek-chat"
    }
  ],
  "activeId": "uuid",
  "webAccess": false
}
```

- `profiles`：一个或多个厂商配置。支持 Anthropic / OpenAI / DeepSeek / MiniMax / Moonshot / Zhipu / Qwen / Doubao / 自定义。
- `activeId`：当前激活的厂商配置。
- `webAccess`：是否允许 AI 助手联网（`true`/`false`）。开启后 AI 可使用 `web_search` / `web_fetch` 工具。

> 配置入口：AI 设置 → AI 配置。可测试连接、拉取模型列表并选择模型。

## 2. 外观主题

- 选项：浅色 / 深色。
- 存储：渲染进程 `localStorage` 键 `aicopilot-theme`（`light` / `dark`）。
- 深色模式下所有显示框统一为黑/深灰底 + 白字。

## 3. 权限模式（AI 助手）

- 取值：`ask`（默认，每次操作询问）/ `trust`（本次会话自动放行）/ `deny`（只读，全部拒绝）。
- 在 AI 助手输入区下拉切换，仅影响当前会话的写/改/打开类操作授权。

## 4. 技能（skills）

- 内置技能 + 外部技能存于 `~/Library/Application Support/AI Copilot/skills/<name>/SKILL.md`。
- 推荐技能（PDF 工具包、浏览器自动化、深度研究、批量处理、文档翻译、代码精简）在 AI 设置 → 智能体技能 → 推荐技能 中一键安装。
- AI 执行任务时若需要未安装的能力，会弹窗请求授权后自动安装。

## 5. 自动更新

- 更新清单：`latest.json`（仓库根，公开可读）。
- 检查地址：`https://raw.githubusercontent.com/ddxmu/AI-Copilot/main/latest.json`
- 行为：启动静默检查；AI 设置 → 软件更新 可手动「检查更新」并「下载并安装」。
- 下载源：GitHub Releases 中对应版本的 DMG 安装包（公开，无需登录）。

## 6. 文件自动化预设

路径：`~/Library/Application Support/AI Copilot/automation-presets.json`
保存「文件自动化」模块中定义的编写规范预设（可存/调用/删除）。

## 7. 对话历史

路径：`~/Library/Application Support/AI Copilot/chat-history.json`
保存 AI 助手的多轮对话记录（标题取首条用户消息），支持切换/改名/归档/删除。
